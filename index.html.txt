<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hector Sport - Quad Cortex Presets</title>
    <meta name="description" content="Professional Quad Cortex presets for live and studio use.">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-image: url('IMG_1386.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-color: #0a0a0a;
            color: #e0e0e0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            min-height: 100vh;
            line-height: 1.6;
        }

        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.68);
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 820px;
            padding: 20px;
        }

        h1 {
            font-size: 4.2rem;
            margin-bottom: 16px;
            font-weight: 700;
        }

        .subtitle {
            font-size: 1.35rem;
            color: #ccc;
            margin-bottom: 32px;
        }

        .test-success {
            color: #ffcc00;
            font-size: 2rem;
            margin: 20px 0;
            font-weight: bold;
        }

        /* Rest of the styles remain the same... */
        .btn { display: inline-block; padding: 16px 38px; border: 2px solid #fff; color: white; text-decoration: none; font-weight: bold; font-size: 1.1rem; transition: all 0.3s ease; border-radius: 4px; }
        .btn:hover { background: white; color: black; transform: translateY(-3px); }

        .products { padding: 110px 20px; max-width: 1200px; margin: 0 auto; background: rgba(10, 10, 10, 0.97); }
        .section-title { text-align: center; font-size: 2.8rem; margin-bottom: 60px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 32px; }
        .card { border: 1px solid #333; padding: 35px 25px; text-align: center; background: #111; border-radius: 8px; transition: transform 0.3s ease; }
        .card:hover { transform: translateY(-8px); }
        .price { font-size: 2.1rem; font-weight: bold; margin: 20px 0; color: #fff; }
        .buy-btn { display: inline-block; padding: 14px 32px; background: white; color: black; text-decoration: none; font-weight: bold; border-radius: 4px; transition: all 0.3s ease; }
        .buy-btn:hover { background: #ddd; transform: scale(1.05); }

        .contact { padding: 100px 20px; background: rgba(10, 10, 10, 0.98); text-align: center; }
        .social-links { display: flex; justify-content: center; gap: 40px; flex-wrap: wrap; margin-top: 30px; }
        .social-links a { color: #e0e0e0; font-size: 2.2rem; transition: all 0.3s ease; text-decoration: none; }
        .social-links a:hover { color: #ffcc00; transform: translateY(-5px); }

        footer { padding: 50px 20px; text-align: center; color: #777; border-top: 1px solid #222; background: rgba(10, 10, 10, 0.95); font-size: 0.95rem; }

        @media (max-width: 768px) {
            h1 { font-size: 3rem; }
            body { background-attachment: scroll; }
        }
    </style>
</head>
<body>

    <section class="hero">
        <div class="hero-content">
            <h1>Quad Cortex Presets</h1>
            <p class="subtitle">Professional tones for live performances and studio sessions.<br>Built to cut through the mix and feel like a real amp.</p>
            
            <!-- TEST WORD - YOU SHOULD SEE THIS -->
            <p class="test-success">✅ TEST SUCCESSFUL - NEW VERSION IS LIVE</p>
            
            <a href="#products" class="btn">Shop Presets</a>
        </div>
    </section>

    <section id="products" class="products">
        <h2 class="section-title">Preset Packs</h2>
        <!-- Cards stay the same -->
        <div class="grid">
            <div class="card">
                <h2>Cleans Pack</h2>
                <p>Crystal clear ambient and edge-of-breakup tones.</p>
                <div class="price">$19.99</div>
                <a href="#" class="buy-btn">Buy Now</a>
            </div>
            <!-- Other cards omitted for brevity, but they are still there in the full version -->
        </div>
    </section>

    <section class="contact">
        <h2 class="section-title">Get in Touch</h2>
        <p style="max-width: 600px; margin: 0 auto 40px; font-size: 1.2rem; color: #ccc;">
            Have questions about the presets or want custom tones? Reach out!
        </p>
        
        <div class="social-links">
            <a href="https://www.instagram.com/hectorsport/" target="_blank" title="Instagram">📷</a>
            <a href="mailto:hectorsport@icloud.com" title="Email">✉️</a>
        </div>
    </section>

    <footer>
        © 2026 Hector Sport. All Rights Reserved.
    </footer>

</body>
</html>
 
       
         
